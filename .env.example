# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# Required: the MiniMax API endpoint URL (keep this secret)
MINIMAX_ENDPOINT=

# Optional: bearer token for the endpoint (if your endpoint requires one)
MINIMAX_API_KEY=
